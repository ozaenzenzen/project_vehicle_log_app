import 'package:flutter/material.dart';

class AppLineFieldWidget extends StatelessWidget {
  const AppLineFieldWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
